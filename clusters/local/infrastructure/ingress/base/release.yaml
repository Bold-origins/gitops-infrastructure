apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ingress-nginx
  namespace: ingress-nginx
spec:
  interval: 5m
  chart:
    spec:
      chart: ingress-nginx
      version: "4.4.x"
      sourceRef:
        kind: HelmRepository
        name: ingress-nginx
        namespace: flux-system
  install:
    crds: Skip
  values:
    controller:
      service:
        type: LoadBalancer
      metrics:
        enabled: false
      resources:
        requests:
          cpu: 100m
          memory: 128Mi
  postRenderers:
    - kustomize:
        patches:
          - target:
              kind: ServiceMonitor
              name: .*
            patch: |
              $patch: delete
              kind: ServiceMonitor
              metadata:
                name: placeholder 