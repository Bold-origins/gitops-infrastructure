# This file will be generated by the Flux bootstrap process
# This is a placeholder file for demonstration purposes

apiVersion: v1
kind: Namespace
metadata:
  name: flux-system
  labels:
    app.kubernetes.io/instance: flux-system
    app.kubernetes.io/part-of: flux
    app.kubernetes.io/version: v2.0.0
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: gitrepositories.source.toolkit.fluxcd.io
spec:
  group: source.toolkit.fluxcd.io
  names:
    kind: GitRepository
    plural: gitrepositories
    shortNames:
      - gitrepo
  scope: Namespaced
  versions:
    - name: v1
      served: true
      storage: true
# ... Additional CRDs and components would be here in the actual file
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: source-controller
  namespace: flux-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: source-controller
  template:
    metadata:
      labels:
        app: source-controller
    spec:
      containers:
        - name: manager
          image: ghcr.io/fluxcd/source-controller:v1.0.0
          imagePullPolicy: IfNotPresent
# ... Additional deployments and components would be here in the actual file
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kustomize-controller
  namespace: flux-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kustomize-controller
  template:
    metadata:
      labels:
        app: kustomize-controller
    spec:
      containers:
        - name: manager
          image: ghcr.io/fluxcd/kustomize-controller:v1.0.0
          imagePullPolicy: IfNotPresent
# ... Additional deployments and components would be here in the actual file 